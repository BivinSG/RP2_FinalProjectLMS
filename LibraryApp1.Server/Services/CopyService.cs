using LibraryApp1.Server.Interfaces.IRepositories;
using LibraryApp1.Server.Interfaces.IServices;
using LibraryApp1.Server.Repositories;

namespace LibraryApp1.Server.Services
{
    public class CopyService //: ICopyRepository1
    {
        //private readonly ICopyRepository _copyRepository;


       
        //public void ReserveCopy(int copyId)
        //{
        //    var copy = _copyRepository.GetById(copyId);
        //    if (copy == null) throw new Exception("Copy not found");

        //    if (copy.isavailiable == 0)
        //        throw new Exception("Copy already issued");

        //    if (copy.isavailiable == 2)
        //        throw new Exception("Copy already reserved");

        //    copy.isavailiable = 2; // reserved
        //    _copyRepository.Update(copy);
        //}

    }
}
